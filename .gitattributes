# Use union merge for PROGRESS.md to avoid conflicts when parallel branches
# both append work log entries. Git will include all lines from both sides.
PROGRESS.md merge=union

# docs/ is auto-generated by build_site.yml — collapse in PR diffs and exclude
# from language statistics so GitHub does not count generated HTML as code.
docs/**  linguist-generated=true
docs/**  -diff
