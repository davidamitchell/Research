{
  "name": "research-site-search",
  "private": true,
  "version": "0.1.0",
  "description": "Build vector search runtime assets for the Research static site",
  "scripts": {
    "build:search-assets": "node scripts/search/build_search_assets.mjs",
    "test:ui": "playwright test"
  },
  "dependencies": {
    "@orama/orama": "^3.1.18",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "esbuild": "^0.25.5"
  }
}
