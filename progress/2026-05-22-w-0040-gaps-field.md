# 2026-05-22 -- W-0040 gaps field and aggregate_gaps.py

**Completed:**

- `Research/_template.md` — added `gaps: []` frontmatter field with comment referencing `aggregate_gaps.py`
- `src/research/item.py` — added `gaps: list[str]` field to `ResearchItem` dataclass; defaults to `[]` when absent from frontmatter
- `scripts/aggregate_gaps.py` — new script that scans `Research/completed/`, normalises and counts gap strings, writes `state/gap_registry.json` with `promote: true` for gaps appearing in ≥3 items; full CLI support
- `research-prompt.md` — added Step 0 (check gap registry for promoted gaps at session start) and `gaps:` instruction in the frontmatter population list (after §6 Synthesis)
- `tests/test_aggregate_gaps.py` — 21 unit tests covering extraction, normalisation, counting, threshold promotion, schema, and file output
- `tests/test_research_item.py` — 3 new tests covering `gaps:` parsing, default when absent, default when empty

## Mini-Retro

1. **Did the process work?** Yes. swe → tdd → code-review loop ran cleanly. Tests written first (Red), script implemented (Green), lint fixed, code-review applied, all 576 tests pass.

2. **What slowed down or went wrong?** Three minor lint issues: unused variable (`slug_list`), unused import (`pytest`, `defaultdict`), and SIM108 ternary suggestion. All caught by `make check` before committing.

3. **What single change would prevent this next time?** Reviewing imports one more time before writing the test file would have caught the unused `pytest` import. Not a significant issue.

4. **Is this a pattern?** Leftover boilerplate imports (like `import pytest` copied from a similar test file) appear occasionally. The lint step catches them — no structural change needed.

5. **Does any documentation need updating?** The `gaps:` field instruction in `research-prompt.md` is the primary documentation. `Research/_template.md` has the comment. No other docs affected.

6. **Do the default instructions need updating?** No new conventions emerged; W-0040 is self-contained.
