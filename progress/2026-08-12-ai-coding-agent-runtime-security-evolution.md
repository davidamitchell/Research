# 2026-08-12 -- Add backlog item (ai-coding-agent-runtime-security-evolution)

**Completed:**
- `Research/backlog/2026-08-12-ai-coding-agent-runtime-security-evolution.md` -- added from issue #642; consolidates the two proposed prompts into one scoped research question on the security-and-assurance-driven evolution of AI coding-agent runtimes from local sandboxes to shared CI/cloud infrastructure to purpose-built multi-tenant platforms.

## Mini-Retro

1. **Did the process work?** Yes. The issue already contained a strong refined question and source seeds, so the main task was to normalise it into the repository template with explicit scope, approach decomposition, and URL-backed source entries.
2. **What slowed down or went wrong?** The issue listed seed sources mostly by name without URLs, so adding verifiable links required extra curation and conservative source selection.
3. **What single change would prevent this next time?** Add issue-template guidance that every proposed seed source should include a URL at submission time.
4. **Is this a pattern?** Yes. New research requests often include high-quality source names but not direct links, which creates avoidable rework when converting to backlog items.
5. **Does any documentation need updating?** Not required for this slice; current templates already enforce URL-based sources and clear scope structure.
6. **Do the default instructions need updating?** No immediate update required; current instructions already specify URL requirements and the new-research-request workflow clearly.
