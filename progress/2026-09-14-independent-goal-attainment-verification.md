# 2026-09-14 -- Independent goal-attainment verification (research item)

**Completed:**
- `Research/completed/2026-08-20-independent-goal-attainment-verification.md` -- moved from `backlog` through `in-progress` to `completed`. Answers: reusable, content-pool-independent goal-attainment verification primitives are cryptographic provenance attestation, append-only transparency logging, runtime verification monitors, and zero-knowledge proof, evaluated against Goguen-Meseguer (1982) non-interference as a graded property rather than a binary one.
- Ran the full `research` skill process (§0 Initialise through §7 Recursive Review), seeded `## Findings` from `§6 Synthesis` (Executive Summary, 12 Key Findings, 12-row Evidence Map, 3 Assumptions, Analysis, Risks/Gaps, Open Questions, Output).
- Extended `learnings.md` Thread 11 (volume-correctness inversion) with a new paragraph and evidence bullet on the primitive-level mechanism inventory this item establishes.

**Review cycle:**
- Draft committed (`faf719b`), review triggered. Pass 1 (`review_count: 0 -> 1`) failed with 3 violations: bare "SLSA" in a Sources-bullet description before its first prose expansion, all three Assumptions paragraphs with an unlabeled opening sentence, and the `related:` item `2026-04-28-llm-as-judge-pipeline-validation-checkpoints` never engaged with in body prose. Fixed all three, recommitted, re-triggered.
- Pass 2 (`review_count: 1 -> 2`, the workflow's final pass) failed with 4 violations: "METR" never expanded to its full organisation name (Model Evaluation and Threat Research), two `§2i` sentences and one Findings Analysis sentence that bound a source-supported clause and this item's own extrapolated clause under a single `[fact]` label instead of splitting the extension into its own `[inference]`. Fixed all four, recommitted.
- Triggered a third pass to check the log even though `review_count` had already reached the workflow's cap (2) and the item was allowed to proceed regardless. That pass still failed on one new violation (bare "AI" in a `## Related` internal link title before its first expansion two lines later) -- fixed it before calling `research complete`, even though it was not required for the item to advance.
- Ran `python -m src.main research complete 2026-08-20-independent-goal-attainment-verification.md` (bare filename, not the full path) to move the item to `Research/completed/`.

## Mini-Retro

1. **Did the process work?** Yes. Three review passes surfaced seven total violations across two failed runs plus one voluntary extra check; all were fixed before completion, and the final item is internally consistent (medium confidence throughout, no unlabeled claims found on the last self-scan).
2. **What slowed down or went wrong?** Each review pass took roughly 3-4 minutes end-to-end (trigger, wait, log-grep), and two of the three violation classes were ones the self-review checklist in `research-prompt.md` already explicitly names (out-of-order acronym expansion in a Sources-bullet description; Assumptions-paragraph opening sentences left unlabeled; unintegrated `related:` frontmatter items). The self-review pass I ran before the first commit did not catch all instances of patterns it was designed to catch -- it checked representative examples rather than exhaustively grepping every Sources bullet and every Assumptions paragraph.
3. **What single change would prevent this next time?** Before the first draft commit, run the acronym-audit and paragraph-opening-label checks as literal `grep`-based sweeps over the *entire* file (not just the sections most likely to contain violations), the same way this session did only after the first review failure. The checklist already says to do this; the discipline of actually running the grep commands, not just mentally scanning, is the missing step.
4. **Is this a pattern?** Yes -- both the acronym-ordering and Assumptions-opening-sentence failures exactly match rows already in the `.github/copilot-instructions.md` Known Recurring Failure Patterns table. No new pattern needed to be added for those two. The METR-unexpanded and split-epistemic-label violations found on pass 2, and the bare-"AI"-in-Related-link-title violation found on the voluntary third pass, are variants of the same two documented pattern classes (acronym expansion order; over-broad `[fact]` labels spanning a cited clause and an extrapolated clause) rather than new classes, so again no new table row is warranted -- but see below for one small process note.
5. **Does any documentation need updating?** No new row added to the Known Recurring Failure Patterns table, since all violations this session were instances of already-documented classes. One process observation worth naming explicitly here rather than in the table: the workflow's own "pass 2 of 2 -- no issue raised" behavior means a failing final pass does not block completion, so treating the third voluntary review as informational only (not gating) was correct per the prompt's own instructions, and this session followed that path correctly.
6. **Do the default instructions need updating?** No. The existing self-review checklist (Step 6) already lists every check that would have caught these violations pre-commit; the gap was in exhaustive execution of those checks, not in their coverage. Future sessions should treat "run the acronym/label checks with `grep`, not by eye" as the operative interpretation of the existing instructions rather than adding new text.
