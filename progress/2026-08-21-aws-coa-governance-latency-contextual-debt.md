# 2026-08-21 -- Complete research item: AWS COA governance latency and contextual debt

**Completed:**
- `Research/completed/2026-08-20-aws-coa-governance-latency-contextual-debt.md` -- moved from `Research/in-progress/` to `Research/completed/`, `status: completed`, `completed: 2026-08-21T07:48:00+00:00`.
- Full `## Research Skill Output` (§0-§7) and `## Findings` populated with primary-source evidence from AWS's Context Ontology Accelerator (COA) GitHub repository (README files, `proposals.py` source code), an AWS general-availability announcement, a secondary architecture blog (clawaws.com), the Model Context Protocol (MCP) specification, and academic sources for the stability-plasticity dilemma and ontology-quality tooling (HermiT, OntoQA, OOPS!).
- Cross-referenced six prior completed items: `aws-agentcore-knowledge-context-layer`, `tbox-abox-graphrag`, `hybrid-memory-integration-ontology-llm-weights`, `human-in-the-loop-ai-automated-workflows`, `knowledge-curation-governance-for-regulated-ai`, and `ontology-landscape-for-curated-enterprise-context` (added during review remediation to satisfy cross-item-integration).
- Updated `learnings.md` Thread 26 with a new evidence bullet identifying COA's mandatory human accept-gate as a third substitute strategy for classical truth maintenance, alongside engineered detect-then-resolve pipelines and production memory-system heuristics, and extended the thread's open question to a cross-architecture review-latency benchmark gap.

**Key finding:** The research question's premise, that manual Web Ontology Language (OWL)/Shapes Constraint Language (SHACL) verification drives governance delay, does not match COA's unstructured induction path: the package documentation states the automated validation subsystem (HermiT, OntoQA, OOPS!) "is not invoked" for that path, so the actual gate is unassisted human review of an unvalidated proposal. Contextual debt is architecturally guaranteed (an in-flight guard blocks new induction while any proposal in a namespace is unresolved, and Serve queries only the accepted graph), but its magnitude depends on an unmeasured variable, human review turnaround time, that no consulted source quantifies for this one-month-old product.

**Review outcome:** Three review passes were run. Pass 1 (`review_count: 0 -> 1`) failed on citation-discipline (SPARQL, Cedar, Turtle domain terms unlinked), speculation-control (an evidence-sufficiency judgment mislabeled `[fact]`), peer-reviewer (four Key Findings assigned high confidence on single or same-organisation sources), and cross-item-integration (a `related:` item never discussed in body). Pass 2 (`review_count: 1 -> 2`) failed on a fresh set of violations surfaced only after the first round of fixes: an unexpanded "AWS" acronym, a bare "HITL" abbreviation in the Evidence Map, an unlabeled Access-note-style sentence, and five more unlabeled paragraph-opening or paragraph-closing interpretive sentences plus three banned adverbs ("simply", "actually", "genuinely"). Both were fixed and pushed. A third invocation of `research-review.yml` (with `review_count` already at the 2/2 cap) auto-passed per the workflow's cap-reached branch, without a further commit.

## Mini-Retro

1. **Did the process work?** Yes. The full research → self-review → draft → automated review → fix → complete pipeline ran end to end and produced a well-sourced item. The self-review pass caught the majority of formatting issues (em-dashes, acronym ordering, bold rules) before the first automated review, but automated review still found 4 categories of substantive violations on pass 1 and a distinct set of 7 on pass 2 that the self-review checklist did not explicitly enumerate.

2. **What slowed down or went wrong?** Fixing violations from pass 1 (adding new inline citations for Cedar, Turtle, SPARQL, and a new related-item cross-reference) introduced brand-new prose that itself contained fresh, unlabeled interpretive sentences, an acronym expanded on the second-fixed sentence but not the original first-use location, and a bare "AWS"/"HITL" that had been present since initial drafting but never caught. This is a recurring shape of failure: fixing a flagged violation by adding new sentences creates a new surface area for the same class of violation, and the self-review pass does not re-scan edited regions with the same rigor as the original draft.

3. **What single change would prevent this next time?** After making any post-review fix that adds or rewrites prose (not just metadata), re-run the full self-review checklist (acronym ordering, per-sentence labeling, banned adverbs, domain-term clarity) on the *edited passage specifically*, not just a diff-level sanity check, before pushing the fix commit. Treat "I just fixed a citation-discipline violation" as a trigger to re-run speculation-control and remove-ai-slop checks on the same paragraph, since the three companion skills interact and a fix for one commonly introduces a violation of another.

4. **Is this a pattern?** Yes, partially. The existing Known Recurring Failure Patterns table documents many individual acronym/labeling violation shapes, but does not yet capture the meta-pattern that *post-review remediation edits are themselves a common source of new violations*, distinct from the original draft's violations. This session's pass-1-to-pass-2 delta (entirely new violation types, not repeats of pass-1 violations) is direct evidence for that meta-pattern.

5. **Does any documentation need updating?** Yes -- see the new Known Recurring Failure Patterns row added to `.github/copilot-instructions.md` in this same session, capturing the "fixing a violation introduces a new one" pattern.

6. **Do the default instructions need updating?** Added a table row (see below); no other changes needed this session.
