# 2026-08-21 -- Complete research item: graphrag-macro-level-hallucination

**Completed:**
- `Research/completed/2026-08-20-graphrag-macro-level-hallucination.md` -- moved from backlog to in-progress to completed in this session; answers whether the noisy baseline produced by unconstrained entity extraction in schema-free GraphRAG pipelines corrupts hierarchical community summaries into a "macro-level hallucination."
- Ran the full research skill (SS0-S7) with primary-source evidence gathering: fetched and read the original GraphRAG paper (Edge et al. 2024), Microsoft's official GraphRAG indexing dataflow documentation, the Leiden community-detection algorithm's primary paper (Traag, Waltman, van Eck 2019), GraphRAG-Bench (Xiang et al. 2025), CS-RAG (2026), an attention-pattern hallucination-detection paper (2025), FinReflectKG-HalluBench (2026), and a systematic RAG-versus-GraphRAG evaluation (Han et al. 2025), plus one practitioner blog treated strictly as an unvalidated `[assumption]` source.
- Cross-referenced and substantively engaged all three `cites:` and all three `related:` completed items in body prose with URL citations, not just Sources-list entries.
- Established the central chained finding: default entity merge is exact-string matching with no semantic resolution -> Leiden clustering guarantees only topological connectivity, not semantic correctness -> the only query-time filter screens for relevance, not correctness -> macro-level hallucination is a structurally-available failure mode, though its real-world occurrence rate is an unmeasured open question (largest identified evidence gap).
- Updated `learnings.md` Thread 11 (volume-correctness inversion) with a new knowledge-graph-construction corollary: no stage of the standard GraphRAG pipeline verifies semantic correctness of merged/clustered entities, extending the thread's generation-cheap/verification-scarce pattern into knowledge-graph construction.

**Review outcome:** Two review passes both returned `OVERALL: FAIL` (review cap is 2, so the item auto-proceeds to completion per the workflow's escalation rule). Violations found and fixed across both passes:
1. Pass 1: Key Findings 1-3 were labelled "high confidence" while each cited only a single source; downgraded to "medium confidence" in both `## Findings` and `SS6 Synthesis`, including matching Evidence Map rows.
2. Pass 2: (a) a sentence in the SS5 Economic lens paragraph mislabelled an evaluative/prescriptive "should weigh" judgement as `[fact]` instead of `[inference]` -- split into a `[fact]` clause and a separate `[inference]` clause; (b) the CS-RAG acronym expansion at first prose use was reversed ("(CS-RAG, Full Name)" instead of "Full Name (CS-RAG)") -- corrected to the required order; (c) RAPTOR was used unexpanded in SS2 Investigation 3b -- expanded to "Recursive Abstractive Processing for Tree-Organized Retrieval (RAPTOR)" at first use (this last violation was fixed for quality even though review_count had already reached the cap and completion could proceed regardless).

**Self-review fixes applied before the first draft commit** (found via a systematic grep-based pass, not caught by external review): all em-dashes replaced with colons (matching the existing convention in completed items) across Sources bullets and template scaffolding lines; acronym-expansion ordering corrected for LLM, RAG, TBox, ABox, AWS, QA, MCC, KG (each was previously expanded later in the document than its first bare use in the pre-existing Scope/Context/Sources template sections); redundant re-expansions in SS0 simplified since Scope now expands them first; a plain-prose "Access note" sentence rewritten as a pure metadata fragment; a leftover template placeholder line removed from the `## Output` section; `output: [knowledge]` frontmatter field set (was left as `[]`); a missing Evidence Map row (HalluBench/MCC finding) added to SS6 Synthesis to restore Findings/Synthesis parity.

## Mini-Retro

1. **Did the process work?** Yes. The mandated self-review grep passes (em-dash, acronym-ordering, prefix/suffix label style, Findings/Synthesis parity) caught nine real defects before the first external review, and the two-pass external review caught three further defects (single-source high-confidence labels, a mislabelled evaluative sentence, a reversed acronym-expansion order) that the self-review checklist did not explicitly enumerate as a targeted check.
2. **What slowed down or went wrong?** The self-review checklist as written checks "is every acronym expanded at first use" but does not include an explicit "is the expansion order Full Name (ACRONYM), never (ACRONYM, Full Name)" sub-check, and it does not include an explicit "does every high-confidence Key Finding cite at least two independent sources" sub-check, even though both are already documented failure patterns in `.github/copilot-instructions.md`. The self-review pass in this session re-derived both checks from first principles rather than finding them pre-encoded as a grep-able rule.
3. **What single change would prevent this next time?** Add two concrete, copy-pasteable grep commands to the research-loop prompt's Step 6 checklist: one for reversed-acronym-order detection (`grep -noE "\([A-Z]{2,}(-[A-Z]{2,})?,\s*[A-Z][a-z]" <item>`) and one for high-confidence single-source detection (count distinct hostnames per Key Finding's source list and flag any "high confidence" row with fewer than two).
4. **Is this a pattern?** Yes -- both of these are already listed as separate entries in the Known Recurring Failure Patterns table in `.github/copilot-instructions.md` (reversed acronym order is a new variant not yet in the table; single-source high-confidence is already there). This session adds the reversed-acronym-order variant as a new, closely related failure mode to that same table.
5. **Does any documentation need updating?** Yes -- see the `research-prompt.md` edit below and the new Known Recurring Failure Patterns table row added to `.github/copilot-instructions.md` in this same commit.
6. **Do the default instructions need updating?** Yes, applied directly (see below) rather than deferred.

## Applied improvements (this session, not deferred)

- Added a new row to the Known Recurring Failure Patterns table in `.github/copilot-instructions.md`: acronym expansions written in reversed order, "(ACRONYM, Full Name)" instead of "Full Name (ACRONYM)", pass the presence-of-expansion self-review check but still fail external review, which checks direction, not just presence.
- Added two concrete grep commands to `research-prompt.md`'s Step 6 acronym-expansion and high-confidence checklist items so future sessions can run them directly rather than re-deriving the check from the general instruction.
