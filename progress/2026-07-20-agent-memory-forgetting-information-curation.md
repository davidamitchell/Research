# 2026-07-20 -- Agent memory forgetting and information curation

**Completed:**
- `Research/completed/2026-07-20-agent-memory-forgetting-information-curation.md` -- researched autonomous forgetting policies, retention scoring signals, hot-path vs. background curation, and failure modes for agent memory systems. Cross-referenced four prior completed items (agent memory taxonomy, RAG/neuroscience memory synthesis, regulated-AI knowledge governance, episodic-to-semantic consolidation) to scope this item to the write-path retention-decision layer. Investigated GitHub Copilot's production memory system, Letta's eviction-with-summarisation architecture, Yodaplus's practitioner refresh triggers, LangChain's context-engineering framework, and three research-grade systems (Memory-R1, EMG-RAG, AssoMem) plus two academic surveys. Confidence: medium.
- `learnings.md` -- extended Thread 5 (agent memory architecture mirrors organisational knowledge management) with this item's evidence bullet and a sentence on the practitioner/research-tier split and the missing audited-retirement-step gap.

## Mini-Retro

1. **Did the process work?** Yes, but it took two full review cycles to pass the automated quality gate, both failing on citation-discipline and speculation-control patterns that are already documented in `copilot-instructions.md`'s Known Recurring Patterns table. The underlying research (source discovery, fact-gathering, synthesis) was sound on the first pass; the failures were entirely in formatting and per-sentence label discipline.

2. **What slowed down or went wrong?** Three things: (a) I initially followed `research-prompt.md`'s own Key Findings template literally, which prescribes full-sentence bold, when the actual review rule (via the `remove-ai-slop` skill) prohibits exactly that -- a live contradiction between the prompt's example and the enforced rule. (b) Multi-sentence bullets/paragraphs (Assumptions, §5 Depth and Breadth Expansion lenses) inherited a single trailing label from the first sentence, but review treats every sentence independently -- this is already documented as a recurring pattern but I still missed it on the first self-review pass because the prompt's own Assumptions template uses the same `**Assumption:** ... **Justification:**` bolded two-clause form that got flagged as a prohibited inline-header list. (c) A vague quantifier ("most current... designs") attributed a single vendor's own framing as an unqualified `[fact]`, which needed downgrading to `[inference]` with an explicit note that the "most" scope was the item's own extrapolation, not the source's claim.

3. **What single change would prevent this next time?** Fix `research-prompt.md` itself: (a) change the Key Findings format example to show an unbolded claim sentence with only the trailing confidence/source parenthetical bolded-free, matching the actual enforced rule, and (b) change the Assumptions section example to plain-prose sentence pairs instead of the bolded `**Assumption:**/**Justification:**` two-clause form. Both edits are applied in this session (see below) so the next research-loop run starts from a corrected template.

4. **Is this a pattern?** Yes, on two counts. The Key-Finding-bold pattern is already row 1 in the Known Recurring Patterns table, but the root cause, the prompt's own template contradicting the rule, was never fixed at the source; this session fixes it. The multi-sentence-inherits-one-label pattern is documented for §5 and for closing/opening sentences elsewhere in the table, but the Assumptions section's specific two-clause bold format was not previously called out as its own instance; this is now the same class of problem recurring in a section the table did not yet name explicitly.

5. **Does any documentation need updating?** Yes -- `research-prompt.md`'s Key Findings and Assumptions section examples, updated in this session (see below).

6. **Do the default instructions need updating?** No new row added to `copilot-instructions.md`'s Known Recurring Patterns table this session; both patterns encountered were already documented there. The gap was that `research-prompt.md` (the tuning lever, not the instructions file) still contained a template that actively worked against the documented pattern. That gap is now closed by editing the prompt directly, which is the intended fix path per `research-prompt.md` Step 12.
