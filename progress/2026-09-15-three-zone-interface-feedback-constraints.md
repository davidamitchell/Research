# 2026-09-15 -- Complete research item: three-zone interface feedback constraints

**Completed:**
- `Research/completed/2026-08-20-three-zone-interface-feedback-constraints.md` -- answers how multi-layer architectures (systems engineering, cognitive science, organisational design) separate a content pool, execution environment, and verification mechanism with zone-specific interface constraints. Synthesised six independently developed architectures (MAPE-K, blackboard systems, actor-critic reinforcement learning, NIST Zero Trust, GitOps/Kubernetes admission-policy loops, Team Topologies + sociotechnical systems theory) into a single cross-cutting finding: an "asymmetric write access" constraint, where the execution environment and verification mechanism may both read the shared content pool but neither may write to it directly -- writes must pass through a distinct resolution step.
- `learnings.md` -- added Thread 29 documenting the asymmetric-write-access pattern and its two supporting prior items (`2026-04-27-uelgf-runtime-feedback-loop`, `2026-05-09-hybrid-architecture-probabilistic-llm-deterministic-governance`).

**Sources consulted:** Kephart & Chess (2003) MAPE-K paper; Corkill (1991) "Blackboard Systems"; Sutton & Barto (2018) *Reinforcement Learning: An Introduction* (2nd ed.); NIST SP 800-207 Zero Trust Architecture; CNCF blog (Bugwadia, 2024) on GitOps/admission-policy loops; Martin Fowler's Team Topologies summary; Trist & Bamforth sociotechnical systems theory (via Newcastle University TheoryHub); plus three prior completed repository items used for cross-reference (`2026-04-27-uelgf-runtime-feedback-loop`, `2026-04-26-ai-agent-control-plane-architecture-enterprise`, `2026-05-17-layered-reasoning-state-abstraction-interfaces`, `2026-05-09-hybrid-architecture-probabilistic-llm-deterministic-governance`).

**Review cycle:** Item went through 2 automated review passes plus one auto-pass at the review cap (`MAX_REVIEWS=2`):
1. Pass 1 (first submission): review's own commit failed to push (race with a concurrent workflow commit) -- so per the documented "review commit race" pattern, this did not count toward the pass budget, but its flagged violations were still fixed: a factual error in `§0` about which UELGF item was in `cites:`, an unlabeled historical claim about blackboard architecture's disciplinary origin, unlabeled second sentences in both Assumptions paragraphs, and four Key Findings assigned "high confidence" on a single source each (downgraded to medium).
2. Pass 1 (counted, `review_count: 1`): four newly-introduced violations -- unlabeled closing sentences in Executive Summary, Analysis, and two Risks/Gaps bullets, plus two Key Findings (8 and 9, the item's own cross-architecture synthesis claims) miscalibrated at medium confidence when the evidence map itself said no single source states the generalisation directly (should be low).
3. Pass 2 (counted, `review_count: 2`): citation-discipline/speculation-control/peer-reviewer all passed; `remove-ai-slop` failed on colon/semicolon density in Findings prose (paren-wrapped citation clauses were initially miscounted as prose punctuation, but a real density problem remained after excluding them: ~0.94-5.8 per 150 words depending on measurement method, against a ceiling of 1). Fixed by rewriting two run-on colon/semicolon sentences into separate declarative sentences.
4. Final trigger: `review_count` was already at `MAX_REVIEWS` (2), so the review auto-passed per the workflow's cap logic without a new review pass.

## Mini-Retro

1. **Did the process work?** Yes. The self-review pass (Step 6) caught most mechanical violations (em-dashes, acronym ordering, frontmatter cross-reference gaps) before the first review trigger, and the automated review caught the remaining substantive issues (single-source high-confidence findings, unlabeled closing sentences, colon/semicolon density) across two counted passes plus one uncounted race-condition pass.
2. **What slowed down or went wrong?** The first review run's own commit failed to push because of a race with a concurrent commit on `main` (the exact pattern already documented in the Known Recurring Failure Patterns table). This meant re-diagnosing whether the pass counted before proceeding. Separately, the colon/semicolon density check required writing a custom exclusion regex to distinguish real prose punctuation from the mandated `(label; confidence; source: URL)` parenthetical citation format, because a naive count flagged citation formatting as a style violation.
3. **What single change would prevent this next time?** For the colon/semicolon density check specifically, the `remove-ai-slop` skill's density rule should explicitly document that the required trailing-citation parenthetical format (`([inference]; high confidence; source: URL)`) is exempt from the count, the same way bracket-wrapped `[label; source: URL]` clauses already are. This would save a self-review cycle on any future item with many Key Findings.
4. **Is this a pattern?** The review-commit-race pattern is already documented (three-strikes threshold reached per the existing table entry). The colon/semicolon-exemption gap is new but narrow enough not to warrant a new table row on its own; noting it here for visibility if it recurs.
5. **Does any documentation need updating?** No copilot-instructions changes needed this session -- the one new friction point (parenthetical citation format not exempted from colon/semicolon density) is a one-off measurement nuance, not a recurring failure class yet.
6. **Do the default instructions need updating?** No. All patterns encountered this session were already covered by existing Known Recurring Failure Patterns entries (reversed acronym order, cites/related engagement gap, review-commit race, single-source high-confidence findings).
