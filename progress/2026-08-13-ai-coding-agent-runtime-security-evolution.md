# 2026-08-13 -- Complete research item: AI coding-agent runtime security evolution

**Completed:**
- `Research/completed/2026-08-12-ai-coding-agent-runtime-security-evolution.md` -- researched and completed the item "Secure Runtime Evolution for AI Coding Agents," which asked what the logical progression is in AI coding-agent runtime design from local process/OS sandboxes, through shared CI/cloud development infrastructure, to purpose-built multi-tenant platforms, and how secure-execution principles explain each stage's trade-offs.
- `learnings.md` -- extended Thread 10 ("AI failure root causes divide into two independent classes") with the new item's finding that runtime-isolation strength and policy-enforcement correctness are independent architectural sub-problems within the deployment/architectural failure branch.

**Sources consulted:** GitHub Docs (Copilot cloud agent, Actions secure-use, Codespaces security), AWS Bedrock AgentCore product page and devguide, AWS ML Blog engineering post on AgentCore Runtime, Amazon Bedrock AgentCore Samples (GitHub), Firecracker documentation, Cloudflare's `@cloudflare/computer` announcement post, Cursor's cloud-agent-lessons engineering post, a community-compiled coding-agent sandbox survey (GitHub Gist), and two arXiv papers (2607.05743, the Balkanization of Execution-Security Research systematisation of 39 papers; 2601.16206, on sandbox environments and LLM task performance). Three seeded source URLs had gone dead or been renamed since the item was created (Cloudflare blog slug, `uknighted/awesome-ai-coding-sandboxes` repo, `amazon-bedrock-agentcore-samples` repo) and were corrected with working substitutes, documented inline in `## Sources`.

**Key finding:** Coding-agent runtime design progresses through three architecturally distinct stages (local process/OS sandbox; shared CI/cloud development infrastructure; purpose-built multi-tenant platform), each improving isolation strength and credential-delegation granularity, but an independent systematisation of 39 execution-security papers found 69-98% policy-enforcement bypass rates against real-world denylists that persist regardless of isolation stage -- meaning stronger isolation alone does not constitute "solved" runtime security.

**Review process:** Triggered `research-review.yml` three times. Pass 1/2 failed on citation-discipline (AWS and IDE acronyms used before their documented first-use expansion; RFC never expanded) and peer-reviewer (7 of 12 Key Findings labeled "high confidence" on single-source or same-organisation-source evidence). Fixed both classes of violation and re-triggered; pass 2/2 failed again on a distinct set of issues (VM and SSH unexpanded; an unlabeled/uncited §4 Consistency Check paragraph; a mislabeled `[fact]` claim that was actually the item's own interpretive framing; a missing citation in one §3 Reasoning bullet; three consecutive Assumptions paragraphs opening with an identical phrase, a `remove-ai-slop` violation). Fixed all five issues and re-triggered a third time; the workflow's review cap (2) had been reached, so it auto-passed per its documented safety-valve logic (`OVERALL: PASS`, `NOTE: review cap reached (2/2)`). Moved the item to `Research/completed/` via `python -m src.main research complete`.

## Mini-Retro

1. **Did the process work?** Yes, end to end: prior-work cross-reference, full research skill output (§0-§7), Findings seeding, self-review, draft commit, automated review (three passes), and completion all executed as designed. The review workflow's two-strike-then-auto-pass safety valve worked exactly as documented, preventing an infinite fix-loop while still surfacing two full rounds of real, fixable violations first.

2. **What slowed down or went wrong?** My own pre-commit self-review (Step 6 of the research prompt) caught em-dashes and the most common acronym-expansion misses, but it missed several violation classes that the automated `citation-discipline`, `peer-reviewer`, and `remove-ai-slop` reviewers caught on both passes: (a) acronyms used unexpanded *before* their documented first-use point when the first mention was in an earlier section than I had checked (AWS/IDE/VM/SSH); (b) a "high confidence" label applied to single-source or same-organisation-source Key Findings, which the peer-reviewer's evidence-sufficiency rule explicitly requires ≥2 independent sources for; (c) one paragraph in §4 Consistency Check that I wrote as pure connective narrative and never re-scanned for the label/citation requirement that applies to every other paragraph in §2-§6; (d) a closing clause in the Executive Summary that quietly upgraded my own interpretive framing to `[fact]` by inheriting the label of the sentence before it; (e) three Assumptions paragraphs that all happened to share an identical opening clause, which is a `remove-ai-slop` structural-repetition rule I had not run as an explicit check.

3. **What single change would prevent this next time?** Before the first draft commit, run two additional greps that are not currently named explicitly enough in the self-review checklist: (1) a script that extracts every 2+ uppercase-letter token and its *first line number* across the whole document, sorted, so acronym first-use order is verified programmatically rather than by memory of section order; (2) a script that flags any three or more consecutive bullet/paragraph blocks in the same subsection sharing an identical opening n-gram (the exact `remove-ai-slop` violation found here). Both are cheap, deterministic checks that would have caught these violations before spending two review-workflow cycles on them.

4. **Is this a pattern?** Partially. The acronym-first-use-order violation matches the existing "acronym not expanded on first use" pattern in the Known Recurring Failure Patterns table, but this session's specific failure mode -- expanding an acronym correctly somewhere in the document while an *earlier* section still used it bare -- is a variant not explicitly called out there (existing entries describe missing expansion, not out-of-order expansion). The single-source "high confidence" mislabeling and the repeated-paragraph-opener issue are not yet in that table. Recommend adding an entry: "High confidence assigned to a Key Finding backed by a single source or multiple same-organisation sources" and "Three or more consecutive Assumptions/bullet paragraphs sharing an identical opening clause," since both are concrete, mechanically detectable failure classes distinct from the acronym-expansion pattern already documented.
