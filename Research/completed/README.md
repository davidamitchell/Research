# Completed

Research items that have been finished with findings documented.

Move files here from `../in-progress/` when done. Fill in the `## Findings` and `## Output` sections. Update `status:` to `completed` and set the `completed:` date.
